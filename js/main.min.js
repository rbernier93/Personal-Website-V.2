"use strict";

// Rerun the polyfill on all elements with the data attribute
// objectFitPolyfill();
// Add animation classes to main modules
var $modules = $('.bio, .work, .accent'); // $modules.each((index, elem)=> {
//   $(elem).waypoint({
//     handler(direction) {
//       if (direction === 'down') {
//         $(elem).addClass('js-animate');
//       }
//     },
//     offset: '25%'
//   });
// });